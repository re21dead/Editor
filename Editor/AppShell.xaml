<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Editor"
             x:Class="Editor.AppShell">
    
    <FlyoutItem Title="Редактор" Icon="ret.png">
        <ShellContent ContentTemplate="{DataTemplate pages:MainPage}"/>
    </FlyoutItem>
    <FlyoutItem Title="Просмотр" Icon="br.png">
        <ShellContent ContentTemplate="{DataTemplate pages:Page1}"/>
    </FlyoutItem>
    <FlyoutItem Title="Настройки" Icon="setting.png">
        <ShellContent ContentTemplate="{DataTemplate pages:Page2}"/>
    </FlyoutItem>

</Shell> 